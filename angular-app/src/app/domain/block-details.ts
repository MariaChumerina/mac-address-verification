export interface BlockDetails {
  assignmentBlockSize: string,
  blockFound: boolean,
  blockSize: number,
  borderLeft: string,
  borderRight: string,
  dateCreated: string,
  dateUpdated: string,
}
